# 휴대폰에서 밈 제작 이어하기

Chrome 확장 없이 오리쇼츠 웹앱에서 사용하는 흐름입니다.

1. 로그인한 앱의 5초 숏츠 / 15초 기획 화면을 열면 **주간 대본에서 에피소드 고르기**에 최신 대본이 자동으로 나타납니다. 지정된 Drive 폴더에서 파일명의 날짜가 가장 최신인 문서를 찾습니다. 에피소드 선택 후 **이 에피소드로 기획 준비**를 누르면 길이·제작 방향과 추천 등장인물이 채워집니다. 주인공을 확인하고 기획을 생성합니다. 이전 문서는 접힌 **이전 대본·파일을 직접 가져오기**에서 넣을 수 있습니다.
2. **기록**에서 이번 기획을 선택합니다. **남은 사진 만들기**로 없는 사진만 차례대로 생성합니다. 이전 사진을 다음 장면의 참고로 사용합니다. 생성에는 기존 Gemini 비용이 발생합니다.
3. **2. Kling**에서 각 사진을 저장하고 프롬프트를 복사합니다. Kling은 별도 탭에서 직접 조작합니다. 5초는 시작·마지막 사진과 Clip 1, 2 프롬프트를 순서대로 넣습니다. 15초는 네 영상을 4·4·3·4초 순서로 각각 만듭니다. 에셋·옵션·생성은 직접 확인합니다.
4. Kling에서 받은 영상 파일을 저장한 뒤 **3. 편집 → 이 밈 영상 편집 이어하기**를 누릅니다. 연결된 원본 한글 대본이 있으면 나레이션·자막·첫 화면 문구를 가져옵니다. 같은 기록의 기존 편집이 있으면 그 편집을 이어갑니다. 예전 기록에 주간 대본이 없으면 편집에서 한 번 가져와야 합니다.
5. 영상 파일을 넣고 음성·자동 싱크를 준비한 뒤 검수하고 영상을 완성합니다. 기존 교보 손글씨체를 사용합니다. 등장인물 대사는 Kling 소리를 사용하며 TTS에는 해설만 보냅니다. 완성 MP4를 다운로드하거나 공유·저장합니다. SNS 업로드와 공개 설정은 직접 진행합니다.

## PC와 휴대폰 사이에 옮기기

기록은 계정의 클라우드가 아니라 각 브라우저에 저장됩니다. **다른 기기로 옮기기 → 작업 파일 저장·공유**로 기획·사진·연결된 주간 대본을 `.ori.json` 파일로 옮길 수 있습니다. 새 기기의 **기록 → 작업 파일 가져오기**에서 엽니다. 같은 기록을 다시 가져오면 사본을 만들어 기존 자료를 보존합니다.

이 파일에는 Kling 영상, 생성한 음성, 편집 중인 설정이나 완성 MP4가 포함되지 않습니다. 영상 파일은 별도로 옮깁니다. PC 확장 0.10.0부터 새로 준비한 에피소드는 해당 기록에 원본 주간 대본이 자동으로 연결됩니다. 기존 확장 작업은 해당 에피소드의 **영상 편집으로 이어가기**를 누르면 연결됩니다.

최신 문서 탐색·내용 읽기·에피소드 분리는 AI를 호출하지 않습니다. 서버는 공개된 지정 폴더만 읽으며 로그인된 앱 사용자를 확인합니다. Drive API 사용 설정과 서버 전용 WEEKLY_DRIVE_API_KEY(없으면 GEMINI_API_KEY)가 필요합니다. 최신 문서의 형식이 잘못되면 이전 주 대본으로 바꾸지 않고 오류를 표시합니다. 새 문서가 아직 생성되지 않았다면 마지막 문서의 날짜를 그대로 보여줍니다. 기기 간 작업 자동 동기화는 별개로 포함하지 않습니다.

유료 생성·분석·영상 합성 중에는 화면을 열어두세요. 모바일 운영체제가 브라우저를 중지한 뒤에도 작업이 계속된다고 보장하지 않습니다.

검증: 기획 파일 왕복, 다른 기록 사진 거부, 기존 기록 사본 보호, 주간 대본 분리, 320px·390px 레이아웃과 편집 연결. 실제 휴대폰 공유 메뉴 및 Kling 생성은 수동 확인 대상입니다.
