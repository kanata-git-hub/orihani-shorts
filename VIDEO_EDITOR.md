# 영상 편집

기존 기획·시각화·기록과 분리된 영상 편집 탭입니다. Kling 영상 다운로드 이후 사용합니다.

## 사용 순서

1. 확장의 밈 작업 결과에서 **영상 편집으로 이어가기**를 누릅니다. 해당 에피소드의 한국어 대본이 넘어옵니다. 이전에 편집하던 에피소드는 저장된 편집을 다시 엽니다.
2. 5초는 영상 한 개, 15초는 4·4·3·4초 영상 네 개를 각각 지정합니다.
3. 나레이션 문구와 숫자 발음을 확인하고 목소리를 선택한 뒤 음성을 생성합니다. 실제 길이와 발음은 미리듣기로 확인합니다.
4. 자막 문구와 시작·끝 시각을 확인합니다. 원본 대본의 시간을 가져오며 음성 강제 정렬 기능은 포함하지 않습니다. 시간이 없는 대본은 직접 입력할 수 있습니다.
5. 완성 버튼을 누르고 실제 MP4를 재생·다운로드합니다.

영어는 기본으로 넣지 않습니다. 첫 화면 문구는 0~1초, 화면 위에서25% 지점에 표시합니다. 자막은 아래에서25% 지점에 표시하며 줄바꿈과 공통 글자 크기로 좌우75% 안에 들어가도록 계산합니다.

편집 내용·영상·생성 음성은 현재 브라우저의 IndexedDB에 저장됩니다. 다른 컴퓨터에 자동 동기화하지 않습니다. 최종 파일을 다운로드해 보관하세요. 음성은 대본·목소리·말투·목표 길이가 같으면 재사용합니다. 자막 변경으로 음성을 다시 생성하지 않습니다.

## 서버와 배포

- Dockerfile로 빌드하세요. FFmpeg와 한국어 Noto 글꼴 설치가 포함됩니다. Dockerfile을 사용하지 않는 기존 배포 방식이라면 해당 도구 설치를 별도로 해야 합니다.
- 기존 `GEMINI_API_KEY` 시크릿을 그대로 사용합니다. 새 음성 API는 브라우저로 키를 보내지 않습니다.
- 선택 환경변수 `EDITOR_TTS_MODEL`: 기본 `gemini-3.1-flash-tts-preview`.
- 선택 환경변수 `EDITOR_FIREBASE_PROJECT_ID`: 기본 `gen-lang-client-0165298283` (기존 프런트엔드 인증 프로젝트).
- 선택 환경변수 `EDITOR_ALLOWED_EMAILS`: 편집을 허용할 이메일을 쉼표로 나열합니다. 미설정 시 이 Firebase 프로젝트의 로그인 사용자를 허용합니다.
- Cloud Run 초기 설정 권장값: CPU2, 메모리2GiB, 요청 제한시간300초, 동시 요청1. 실제 사용량을 보고 조절합니다. 기존 환경변수·시크릿 연결을 보존하세요.
- 합계28MB 이하의 파일을 받습니다. 서버는 요청 중 합성하고 MP4를 반환합니다. 임시 파일은 요청 종료 시 정리합니다. 재시도 전에 기존 결과를 확인하세요.
- 편집 엔드포인트는 Firebase 로그인 토큰을 검증합니다. 사용자당/인스턴스당 동시 작업을 제한합니다. 음성 생성 실패는 비용 중복 방지를 위해 자동 재시도하지 않습니다.
- 나레이션이 속도 조절 후에도 영상보다 길면 합성을 중단합니다. 영상 길이에 맞춰 음성을 임의로 자르지 않습니다.
- 배포 후 로그인, 실제 TTS 한 번, 본인 Kling 영상으로 완성본 소리와 자막을 확인해야 합니다. TTS 모델 사용 권한·쿼터는 실제 Google 계정에서 별도 확인이 필요합니다.

## 개발 검증

`npm ci`, `npm run lint`, `npm run build`를 실행합니다.

`node --test tests/editor.test.cjs`는 대본·자막 검사를 실행합니다. `FFMPEG_PATH`에 FFmpeg 실행 파일 경로(또는 `ffmpeg`)를 지정하면 5초·15초 실제 합성 및 음성 트랙 검사도 실행합니다. `.editor-test`는 검사 산출물이며 배포/커밋에서 제외합니다.

UI의 자막 시각은 편집 가능하지만 현재 음성 인식 기반 자동 정렬은 아닙니다. VLLO의 특정 목소리를 복제하거나 동일하다고 보장하지 않습니다.
